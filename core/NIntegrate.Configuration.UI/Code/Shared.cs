namespace NIntegrate.Configuration.UI.Code
{
    internal sealed class Shared
    {
        #region Nested Classes

        public class SharedSettings
        {
            internal SharedSettings()
            {
            }
        }

        #endregion

        public static readonly SharedSettings Settings = new SharedSettings();
    }
}
